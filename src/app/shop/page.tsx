import {products} from '@/data/products';import {ProductCard} from '@/components/storefront';export default function Shop(){return <main className="listing section"><div className="listing-head"><span className="eyebrow">ELVORA / SHOP</span><h1>OBJECTS FOR<br/><i>BETTER LIVING.</i></h1><p>Small things, thoughtfully made.</p></div><div className="filterbar"><span>{products.length} essentials</span><span>ALL / DESK / BEDROOM / KITCHEN</span></div><div className="product-grid">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div></main>}
