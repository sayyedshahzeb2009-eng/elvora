'use client';import {useState} from 'react';import {ArrowUpRight,Minus,Plus,ShoppingBag} from 'lucide-react';import {useCart} from '@/components/cart/cart-provider';import type {Product} from '@/data/products';export function AddToCart({product}:{product:Product}){const {add}=useCart();const [qty,setQty]=useState(1);const [done,setDone]=useState(false);return <div className="purchase"><div className="purchase-qty"><button onClick={()=>setQty(Math.max(1,qty-1))}><Minus/></button><span>{qty}</span><button onClick={()=>setQty(qty+1)}><Plus/></button></div><button className="button dark buy" onClick={()=>{for(let i=0;i<qty;i++)add(product);setDone(true);setTimeout(()=>setDone(false),1600)}}>{done?'ADDED TO BAG':'ADD TO BAG'} <ShoppingBag size={17}/></button><button className="button outline buy-now">BUY NOW <ArrowUpRight size={16}/></button></div>}
