document.addEventListener('DOMContentLoaded',()=>{const header=document.querySelector('[data-header]');let last=0;window.addEventListener('scroll',()=>{const y=window.scrollY;if(header)header.classList.toggle('scrolled',y>30);last=y},{passive:true});document.querySelectorAll('.quick-add').forEach(form=>form.addEventListener('submit',()=>{const button=form.querySelector('button');if(button){const old=button.textContent;button.textContent='ADDED ✓';setTimeout(()=>button.textContent=old,1400)}}));});