document.addEventListener('DOMContentLoaded',()=>{const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const mobile=window.matchMedia('(max-width: 900px)').matches;const q=(s,r=document)=>r.querySelector(s);const qa=(s,r=document)=>[...r.querySelectorAll(s)];if(!reduce){qa('.hero-object,.stage-product,.space-card,.button,.social-tile').forEach(el=>{el.addEventListener('pointermove',e=>{if(mobile)return;const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.setProperty('--mx',(x*10).toFixed(2)+'deg');el.style.setProperty('--my',(y*-8).toFixed(2)+'deg')});el.addEventListener('pointerleave',()=>{el.style.removeProperty('--mx');el.style.removeProperty('--my')})})}const reveal=qa('.el-space,.el-compare,.el-benefits,.el-social,.el-newsletter,.el-section-top,.space-card,.benefit-row,.social-tile');if('IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}}),{threshold:.12});reveal.forEach(x=>x.classList.add('reveal'),io.observe(x))}const range=q('[data-range]'),before=q('[data-before]'),handle=q('[data-handle]'),frame=q('.compare-frame');if(range&&before&&handle&&frame){const set=v=>{before.style.width=v+'%';handle.style.left=v+'%'};range.addEventListener('input',e=>set(e.target.value));frame.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'||e.key==='ArrowRight'){e.preventDefault();const n=Math.max(0,Math.min(100,+range.value+(e.key==='ArrowRight'?3:-3)));range.value=n;set(n)}})}const exp=q('[data-experience]');if(exp&&!reduce){const lines=qa('.exp-line',exp),count=q('[data-exp-count]',exp),product=q('[data-product3d]',exp);const update=()=>{const r=exp.getBoundingClientRect(),p=Math.max(0,Math.min(1,-r.top/(exp.offsetHeight-innerHeight)));const index=Math.min(lines.length-1,Math.floor(p*lines.length));lines.forEach((l,i)=>l.classList.toggle('is-active',i===index));if(count)count.textContent='0'+(index+1);if(product)product.style.transform=`rotateX(${55+p*18}deg) rotateZ(${-18+p*22}deg) translateY(${Math.sin(p*Math.PI)*-22}px)`};addEventListener('scroll',update,{passive:true});update()}const header=q('[data-header]');if(header){addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>24),{passive:true})}});
